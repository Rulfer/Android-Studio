<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/start_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/myPoop"

    tools:context="makingview.com.apkdownloader.StartActivity">

    <Button
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:text="Download Panoaudio"
        android:onClick="downloadPano"
        android:id="@+id/download_pano"
        android:layout_below="@+id/cancel_download"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Download Movie Menu"
        android:onClick="downloadMenu"
        android:id="@+id/download_menu"
        android:layout_below="@+id/download_pano"
        android:layout_alignRight="@+id/download_pano"
        android:layout_alignEnd="@+id/download_pano" />

    <Button
        android:enabled="false"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Install Panoaudio"
        android:onClick="installdPano"
        android:id="@+id/install_pano"
        android:layout_below="@+id/cancel_download"

        android:layout_alignLeft="@+id/install_menu"
        android:layout_alignStart="@+id/install_menu" />

    <Button
        android:enabled="false"
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:text="Install Movie Menu"
        android:onClick="installMenu"
        android:id="@+id/install_menu"
        android:layout_below="@+id/download_pano"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Stop current download"
        android:onClick="cancelDownload"
        android:id="@+id/cancel_download"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:background="@color/myCustom"
        />
</RelativeLayout>